name: Debug Example

on: push

jobs:
  debug-job:
    runs-on: ubuntu-latest

    steps:
    - name: Debug secret
      run: echo "::debug::Secret value is $TOP_SECRET_NUMBER1"
      env:
        TOP_SECRET_NUMBER1: ${{ secrets.TOP_SECRET_NUMBER }} 

# REQUIRES ACTIONS_STEP_DEBUG=true secret